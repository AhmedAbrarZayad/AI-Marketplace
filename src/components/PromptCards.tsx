import React from 'react';
import { Prompts } from '../models/prompts';

interface PromptCards {
    prompt: Prompts;
}

const PromptCards: React.FC<PromptCards> = ({ prompt }) => {
    return (
        <div>

        </div>
    )
}