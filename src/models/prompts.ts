export interface Prompts {
    _id: string;
    title: string;
    description: string,
    category: string,
    model: string,
    difficulty: string,
    author: string,
    createdAt: Date,
    rating: number,
}